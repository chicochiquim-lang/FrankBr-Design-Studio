# Sprint 004 — FB-004 FrankEngine Foundation

## Entrega
- Novo projeto `FrankBr.Engine`.
- `EditorEngine` com ciclo de vida (`Initialize`, `Shutdown`, `Dispose`).
- Contratos `IEditorModule` e `ICanvasModule`.
- Managers iniciais: Canvas, Documents e Tools.
- Integração do `CanvasService` existente por injeção de dependência.
- Encerramento seguro da Engine quando a janela fecha.

## Fora do escopo
- Zoom funcional.
- Pan funcional.
- Undo/Redo.
- Novo formato de documento.
- Mudanças visuais.
