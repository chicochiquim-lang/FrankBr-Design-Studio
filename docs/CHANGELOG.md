# Changelog

## Sprint 003 — PR #003

### Adicionado
- `GridLayer` renderizado com `DrawingContext`.
- Alternância de visibilidade pela toolbar e pelo menu Exibir.
- Integração da grade com `CanvasState.ShowGrid`, `GridSize` e `EffectiveZoom`.
